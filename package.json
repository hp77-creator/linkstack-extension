{
  "name": "linkstack-extension",
  "version": "1.0.0",
  "description": "Browser extension for LinkStash app",
  "scripts": {
    "build:icons": "node scripts/convert-icons.js",
    "build:extension": "node scripts/build.js",
    "build": "npm run build:icons && npm run build:extension"
  },
  "type": "module",
  "devDependencies": {
    "sharp": "^0.32.6"
  }
}
